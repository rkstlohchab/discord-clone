"use client";

interface FileUploadProps {
    onChange: (url?: string) => void;
}

export const FileUpload = () => {
    return (
        <div>
            File Upload Component
        </div>
    )
}